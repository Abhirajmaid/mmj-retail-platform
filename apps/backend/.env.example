# ─── Server ──────────────────────────────────────────────────────────────────
HOST=0.0.0.0
PORT=1337
PUBLIC_URL=http://localhost:1337

# ─── App Keys (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))")
APP_KEYS=appKey1,appKey2
API_TOKEN_SALT=randomsalt
ADMIN_JWT_SECRET=adminJwtSecret
TRANSFER_TOKEN_SALT=transferTokenSalt
# Required for Strapi 5+ (API token visibility, etc.). Generate like APP_KEYS.
ADMIN_ENCRYPTION_KEY=yourEncryptionKeyBase64
# Optional: admin session lifespans in milliseconds (defaults: 7d, 30d)
# ADMIN_SESSION_LIFESPAN_MS=604800000
# ADMIN_REFRESH_TOKEN_LIFESPAN_MS=2592000000

# ─── Authentication ───────────────────────────────────────────────────────────
JWT_SECRET=supersecret

# ─── Database (PostgreSQL — default for team/shared persistence) ─────────────
DATABASE_CLIENT=postgres
DATABASE_HOST=127.0.0.1
DATABASE_PORT=5432
DATABASE_NAME=jewellery_retail
DATABASE_USERNAME=strapi
DATABASE_PASSWORD=strapi
DATABASE_SSL=false
DATABASE_CONNECTION_TIMEOUT=60000

# ─── Database (SQLite fallback for single-dev local) ─────────────────────────
# DATABASE_CLIENT=sqlite
# DATABASE_FILENAME=.tmp/data.db

# ─── GraphQL ─────────────────────────────────────────────────────────────────
GRAPHQL_PLAYGROUND=false
