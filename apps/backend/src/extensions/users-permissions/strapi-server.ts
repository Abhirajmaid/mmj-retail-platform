/**
 * users-permissions extension
 *
 * Extend the built-in users-permissions plugin here.
 * For example: add custom JWT strategies, modify user schema, etc.
 */
export default (plugin: any) => {
  return plugin;
};
